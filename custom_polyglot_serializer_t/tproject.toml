[project]
name = "custom_polyglot_serializer_t"
description = "Extension of the serializer system using the dictionary format and foreign code blocks for polyglot interchange."

[dependencies]

[r-dependencies]
packages = ["yaml", "jsonlite"]

[py-dependencies]
version = "python313"
packages = ["pyyaml"]

[julia-dependencies]
version = "lts"
packages = []

[additional-tools]
packages = ["quarto"]

[latex]
packages = []

[t]
min_version = "0.52.0"

[nixpkgs]
date = ""
