[project]
name = "per_node_flake_t"
description = "Demonstrates per-node flake support in T pipelines with selective fallback"

[r-dependencies]
packages = ["jsonlite"]

[py-dependencies]
packages = []

[jl-dependencies]
version = "lts"
packages = ["JSON"]

[t]
min_version = "0.55.0"
