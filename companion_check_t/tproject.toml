[project]
name = "companion_check_t"
description = "A demo to verify that the tlang companion packages load correctly in R, Python, and Julia"

[dependencies]

[r-dependencies]
packages = ["dplyr", "jsonlite"]

[py-dependencies]
version = "python314"
packages = ["pandas"]

[jl-dependencies]
version = "lts"
packages = ["DataFrames", "JSON", "CSV"]

[additional-tools]
packages = []

[latex]
packages = []

[t]
min_version = "0.55.0"

[nixpkgs]
date = "2026-04-02"
