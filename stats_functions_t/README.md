# stats_functions_t

This demo exercises the T `stats` package end to end by running T nodes first, reproducing the same computations in R nodes, and validating parity with `assert()`.

## Run

```bash
t run src/pipeline.t
```
