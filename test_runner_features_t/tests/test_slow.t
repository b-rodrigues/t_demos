-- test_slow.t — placeholder for a slow-running test
--
-- This file is excluded by tests/.tignore.
-- It demonstrates that .tignore prevents execution of expensive tests
-- during routine `t test` runs.

assert(true)
