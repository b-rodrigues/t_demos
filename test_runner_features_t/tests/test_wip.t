-- test_wip.t — work-in-progress test (intentionally broken)
--
-- This file is excluded by tests/.tignore.
-- If .tignore were removed, this test would fail the suite,
-- proving that .tignore actually prevents execution.

assert(expect_equal(1, 2))
