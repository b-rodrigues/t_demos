# package_manager_functions_t

This demo exercises T package-manager workflows end to end by scaffolding a package and a project, generating docs, running tests, running `t doctor`, syncing with `t update`, and importing the generated package through `T_PACKAGE_PATH`.

## Run

```bash
t run --unsafe src/pipeline.t
```
