[project]
name = "glm_basic_r_t"
description = "GLM Example: Basic R logistic regression"

[t]
min_version = "0.55.0"

[r-dependencies]
packages = ["arrow"]

[additional-tools]
packages = ["jpmml-evaluator", "jre"]
