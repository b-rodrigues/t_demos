[project]
name = "model_comparison_t"
version = "0.1.0"
description = "A demo showcasing model comparison using pipeline operations."

[dependencies]
base = "*"
pipeline = "*"
dataframe = "*"
colcraft = "*"
math = "*"
stats = "*"

[additional-packages]
r = ["dplyr", "readr", "arrow", "jsonlite"]
python = ["scikit-learn", "pandas", "pyarrow"]
