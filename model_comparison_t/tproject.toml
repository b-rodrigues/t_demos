[project]
name = "model_comparison_t"
version = "0.1.0"
description = "A demo showcasing model comparison using pipeline operations."

[dependencies]
base = "*"
pipeline = "*"
dataframe = "*"
colcraft = "*"
math = "*"
stats = "*"

[r-dependencies]
packages = [ "dplyr", "readr", "arrow", "jsonlite",]

[py-dependencies]
version = "python313"
packages = [ "scikit-learn", "pandas", "pyarrow",]

[additional-tools]
packages = [ "quarto",]

[t]
min_version = "0.51.2"
