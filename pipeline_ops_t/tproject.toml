[project]
name = "pipeline_ops_t"
description = "A T project demonstrating advanced pipeline operations"
authors = []

[dependencies]

[r-dependencies]
packages = ["dplyr", "jsonlite", "arrow", "knitr", "rmarkdown"]

[py-dependencies]
version = "python313"
packages = ["pandas", "ipykernel", "nbclient", "nbformat", "pyyaml"]

[jl-dependencies]
version = "lts"
packages = []

[additional-tools]
packages = ["quarto", "which"]

[latex]
packages = []

[t]
min_version = "0.55.0"

[nixpkgs]
date = ""

