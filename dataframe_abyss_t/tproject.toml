[project]
name = "dataframe_abyss_t"
description = "Stress test data frame shapes: empty, all-NA, duplicate names, type coercion, pivot edge cases"

[t-dependencies]
packages = ["core", "dataframe", "colcraft", "base"]

[t]
min_version = "0.55.0"
