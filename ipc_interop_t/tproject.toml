[r-dependencies]
packages = [ "arrow", "reticulate"]

[py-dependencies]
packages = [ "pandas", "pyarrow"]

[project]
name = "ipc_interop_t"
description = "A T data analysis project"

[t]
min_version = "0.55.0"
