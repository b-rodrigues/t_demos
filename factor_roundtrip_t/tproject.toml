[r-dependencies]
packages = [ "arrow",]

[py-dependencies]
packages = [ "pandas", "pyarrow",]

[project]
name = "factor_roundtrip_t"
description = "A T data analysis project"

[t]
min_version = "0.51.2"
