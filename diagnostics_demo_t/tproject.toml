[project]
name = "diagnostics_demo_t"
description = "Observability Demo: Node errors and warnings across T, R, Python, and Julia"

[dependencies]

[r-dependencies]
packages = ["arrow", "jsonlite"]

[py-dependencies]
version = "python314"
packages = ["pandas", "pyarrow"]

[jl-dependencies]
packages = ["DataFrames", "Arrow"]

[additional-tools]
packages = []

[latex]
packages = []

[t]
min_version = "0.55.0"

[nixpkgs]
date = ""
