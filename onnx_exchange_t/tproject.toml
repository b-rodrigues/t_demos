[project]
name = "onnx_exchange_t"
description = "Train models in R and Python, exchange via ONNX, and predict in all runtimes."

[dependencies]

[r-dependencies]
packages = ["onnx", "dplyr", "arrow", "r2pmml"]

[py-dependencies]
version = "python313"
packages = ["onnx", "onnxruntime", "sklearn2pmml", "scipy", "statsmodels"]

[additional-tools]
packages = []

[latex]
packages = []

[t]
min_version = "0.51.2"

[nixpkgs]
date = ""
