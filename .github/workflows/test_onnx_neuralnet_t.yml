name: Test ONNX Neural Network T Pipeline

on:
  push:
    branches: [ master ]
    paths:
      - 'onnx_neuralnet_t/**'
  pull_request:
    branches: [ master ]
    paths:
      - 'onnx_neuralnet_t/**'

jobs:
  test-pipeline:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install Nix
      uses: cachix/install-nix-action@v27
      with:
        nix_path: nixpkgs=channel:nixos-unstable

    - name: Setup Cachix
      uses: cachix/cachix-action@v15
      with:
        name: rstats-on-nix
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'

    - name: Run ONNX Neural Network Pipeline
      working-directory: ./onnx_neuralnet_t
      run: |
        nix develop --command t run src/pipeline.t --unsafe
