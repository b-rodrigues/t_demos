[project]
name = "r_py_xgboost_t"
description = "A T data analysis project"

[dependencies]

[r-dependencies]
packages = ["dplyr", "yardstick", "arrow", "knitr", "rmarkdown"]

[py-dependencies]
version = "python313"
packages = ["numpy", "pandas", "scikit-learn", "xgboost", "pyarrow", "ipykernel", "nbclient", "nbformat", "pyyaml"]

[julia-dependencies]
version = "lts"
packages = []

[additional-tools]
packages = ["quarto", "which"]

[latex]
packages = []

[t]
min_version = "0.52.0"

[nixpkgs]
date = ""
