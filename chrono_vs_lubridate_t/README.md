# Chrono vs Lubridate Demo

This demo compares the `chrono` package in T with the `lubridate` package in R.

## Features tested:
- Date parsing (`ymd`)
- Component extraction (`year`, `month`, `day`)
- Period arithmetic (`+ months(1)`)

## How to run:
```bash
t run src/pipeline.t
```
