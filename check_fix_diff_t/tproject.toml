[project]
name = "check_fix_diff_t"
description = "Demonstrates t check --schema, t fix, and t diff"

[t-dependencies]
packages = ["core", "dataframe", "colcraft", "stats", "pipeline", "testcraft"]

[t]
min_version = "0.55.0"
